---
job-defaults:
    image: {in-tree: python-test}
    optimization:
        skip-unless-changed:
            - "**/*.yml"


yamllint:
    description: "Run `yamllint` to validate the latest changes"
    runner:
        command: yamllint .
