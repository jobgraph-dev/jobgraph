---
job-defaults:
    optimization:
        skip-unless-changed:
            - "**/*.yml"
    runner:
        docker-image: {in-tree: python-test}


yamllint:
    description: "Run `yamllint` to validate the latest changes"
    runner:
        command: yamllint .
