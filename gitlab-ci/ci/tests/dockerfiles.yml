---
job-defaults:
    optimization:
        skip-unless-changed:
            - "**/Dockerfile"


hadolint:
    description: "Run `hadolint` to validate dockerfiles"
    runner:
        command: hadolint $(find . -name 'Dockerfile')
        docker-image: {"docker-image-reference": "<hadolint>"}
