---
job-defaults:
    optimization:
        skip-unless-changed:
        - "**/Dockerfile"


hadolint:
    description: "Run `hadolint` to validate dockerfiles"
    image: {"docker-image-reference": "<hadolint>"}
    script: hadolint $(find . -name 'Dockerfile')
